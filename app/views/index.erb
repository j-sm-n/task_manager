<h1>All Tasks</h1>

<ul class="list-group">
  <li class="list-group-item">
    <span class="badge">New</span>
    <% @tasks.each do |task| %>
      <h3><%= task.id %>: <a href="/tasks/<%= task.id %>"><%= task.title %></a></h3>
      <% end %>
  </li>
</ul>
